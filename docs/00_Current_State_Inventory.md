# 00 — Инвентаризация текущего состояния (Current State Inventory)

> **Метод:** независимый read-only аудит (5 параллельных аудиторов), доказательства = путь:строка кода ИЛИ фактический вывод команды. Ничего не переписывалось. Дата: 2026-07-03.
> **Легенда статусов:** ✅ работает и подтверждено · 🟡 реализовано частично · ⚠️ реализовано, но рискованно · ❌ отсутствует · ❔ не проверено · ➖ не нужно для MVP.
> **Замечание по чистоте аудита:** файл `data/kbtu/search_demo.txt` менялся во время прогона — это был параллельный retrieval-аудитор того же воркфлоу (не внешний процесс, не утечка). Сам факт перезаписи любым запуском `search.py` — отдельная находка (см. R-RET-4).

---

## A. Ingestion — движок A (PDF → чанки)

| Область | Что найдено | Статус | Доказательство | Проблема | Риск |
|---|---|---|---|---|---|
| Загрузка PDF + провенанс | 8 PDF скачаны, manifest с size/hash/timestamp | ✅ | `data/kbtu/raw` = 8 PDF; sha256 hash_match=True для всех 8; `download_kbtu.py:59` проверка `%PDF` | — | — |
| SSL при загрузке | Все 8 файлов скачаны с `verify=False` | ⚠️ | `download_manifest.json`: `ssl_insecure:true` ×8; `download_kbtu.py:34-37` фолбэк | Цепочка сертификатов kbtu не валидировалась ни разу; hash считается от уже скачанного (не защита от MITM) | Отравление корпуса подменённым PDF (для публичных доков низкий) |
| Парсинг PDF (page-aware) | pymupdf4llm, `page_chunks=True` | ✅ | `ingest.py:77`; `ingest_report.json`: 6 PDF → 531 чанк; `wc -l chunks.jsonl`=531 | — | — |
| Таблицы → Markdown (атомарно) | 68 табличных чанков, целые, с префиксом `[Раздел:]` | ✅ | 68/68 начинаются с `[Раздел:`, содержат `|---|`; `ingest.py:149-153` | 6 таблиц >1000 ток (до 1546) — by design | Для эмбеддинга ок (лимит 8191); съедают контекст генератора |
| Метаданные чанка (12 полей) | Все обязательные поля заполнены | ✅ | Скрипт по 531 чанку: missing=0, null=0; пустой `section_title` только 6/531 | 6 чанков без раздела → неполная цитата | Низкий |
| Тип и размер чанкинга vs ТЗ | structure-aware по заголовкам + атомарные таблицы + token-window fallback | 🟡 | `ingest.py:142-153`; параметры `kbtu.yaml`: target 700 / max 1000 / overlap 90 (12.9%) / min 12. **Факт: только 207/531 (39%) в диапазоне 500-1000; <100 ток — 121 чанк; avg=401** | Чанкер НЕ мёржит мелкие секции вверх до target; «семантический» (по embedding-similarity) не реализован — суррогат | Мелкие чанки = бедный контекст, разрыв смысловых связей, больше стоимость |
| Шум: дубли колонтитулов | 46 чанков (8.7%) — точные повторы 5 колонтитулов («**КС ИСМ КБТУ …**») | ⚠️ | Скрипт-дедуп: `59-1-24` ×19, `51-2-25` ×11, `514-1-22` ×9; 16-17 ток > min 12, фильтр не ловит | ~9% корпуса — чистый шум, вытесняет полезные чанки из top-k | Деградация precision retrieval |
| HTML-мусор после `strip_noise` | 5 чанков с оборванным `<mark`, 3 с U+FFFD `�` | 🟡 | Grep: `<mark` ×5 (все 514-1-22), на границе окна; `strip_noise` применяется после нарезки (`ingest.py:99,132`) | Тег/символ, разрезанный границей token-window, выживает половинками | 8 повреждённых чанков в эмбеддингах и цитатах; фикс тривиален |
| OCR календарей | 4 чанка (2 PDF × 2 стр.), conf 0.78-0.84 | 🟡 | `chunks_ocr.jsonl`=4; `ocr_calendars.py:75`; искажения: «недсль», «протокол Ngl2», «АО/КБТУ>» | **`paragraph=False` + построчный join уничтожает табличную структуру календаря — колонки семестров перемешаны, даты рискуют привязаться не к тем событиям**; `ingest_report` не обновлён после OCR; `ocr.log` устаревший (старый traceback) | **Высокий: неверная дата сессии — худшая ошибка для студента** |
| Идемпотентность | `chunk_id=sha1(tenant\|source\|char_start)[:16]`, детерминирован | ⚠️ | `ingest.py:107`; пересчёт совпал, дубли id=0 | `char_start` — синтетический бегущий оффсет, не позиция в документе → смена параметров чанкинга сдвинет все id → сироты | Риск №13 самого дока |

---

## B. Retrieval — движок A (поиск)

| Область | Что найдено | Статус | Доказательство | Проблема | Риск |
|---|---|---|---|---|---|
| Индекс `index.db` | 535 чанков, dim 384, FTS-таблица есть | ✅ | sqlite3: chunks=535, meta dim=384, `chunks_fts` rows=535; tenants: kbtu=535; cat: student 351/abiturient 180/calendar 4 | — | — |
| **Изоляция тенанта (dense) — УТЕЧКА** | Чужие чанки лишь занижаются (-1e9), не исключаются | ⚠️ **БАГ** | `stores.py:97-99` mask+`np.where`; финальная сборка `:116-123` не перепроверяет tenant. **Прогон: `search(tenant_id='other-univ')` вернул 5 чанков kbtu** | Утечка срабатывает всегда, когда у тенанта < k_each(20) чанков (вкл. 0) | **Межвузовая утечка — риск №1 реестра. В однотенантной БД невидима** |
| Изоляция тенанта (lexical/BM25) | Корректный фильтр (исключение) | ✅ | `stores.py:110` | — | — |
| Тест изоляции тенанта | Отсутствует | ❌ | Glob `**/test*` → только `poc/.../test_api.py` (изоляцию не проверяет). `docs/00-foundation.md:500` требует его | Собственный реестр рисков не выполнен именно там, где найден баг | Баг доехал бы до прода незамеченным |
| Гибрид dense+BM25+RRF | Все три компонента на месте | ✅ | Косинус `stores.py:94-96`; `bm25()` `:107`; RRF_K=60 `:16,31` | В SQLite category — ПОСТ-фильтр (для calendar 4 чанка выдача может пустеть) | Деградация recall (не корректности) |
| Цитаты из метаданных | Строятся из полей БД, не из модели | ✅ | `search.py:28-32` (standard_code/стр./раздел/URL); LLM в пайплайне нет | — | — |
| Живой прогон (GPA) | 3 релевантных результата с цитатами | ✅ | `search.py -q "Как рассчитывается GPA?"` → 51-2-25 стр.16/20, 514-1-22 стр.21 | — | — |
| Эмбеддинги: факт vs ADR-007 | MiniLM-384 (демо) вместо OpenAI-3072 | 🟡 | `index.db` dim=384 = `LocalEmbedder` default (`embedding.py:24`); ADR-007 требует 3072. Признано в `retrieval/README.md:29-33`, но НЕ в foundation | MiniLM отсутствует в матрице профилей §5.1 (третий, вне ADR); `search.py` не сверяет `emb.dim==meta.dim` | Запуск `--profile openai` против 384-индекса упадёт невнятной numpy-ошибкой |
| Прод-путь PgVectorHybridStore | Код есть, не запускался | ❔ | `stores.py:128-193`; `psycopg installed: False`, docker off; нет артефактов | «Прод — тот же код» не подтверждён ни одним прогоном | SQL с CTE/tsvector может содержать ошибки |
| `search_demo.txt` перезапись | Пишется при ЛЮБОМ запросе, не только `--demo` | ⚠️ | `search.py:71-72` безусловно | Витринный артефакт затирается ad-hoc запросом | Демо нестабильно |

---

## C. POC движка B — рекомендатель грантов (расширение сверх ТЗ)

| Область | Что найдено | Статус | Доказательство | Проблема | Риск |
|---|---|---|---|---|---|
| Смоук-тесты API | 11 PASS / 0 FAIL | ✅ | `python -m api.test_api` → ALL PASS, exit 0 | Самописный `check()`, не pytest | Низкий |
| Данные + провенанс | 5632 строки, 87 вузов, 2025, полный провенанс | ✅ | rows=5632, unique univ=87; 6 полей провенанса missing=0 в каждой строке | — | — |
| Честность: дисклеймеры | «Не гарантия / исторические» в 3 местах | ✅ | `recommend.py:150`; `api/main.py:58`; `pitch:157-160` | — | — |
| Честность: числа только из БД | В пайплайне нет LLM | ✅ | `recommend.py:77-84` SQL; сверка u50 B057=90/115 совпала | — | — |
| threshold vs passing | Разделены в схеме и выдаче | ✅ | колонка `metric`; `recommend.py:94,137-138` | 1805 threshold-строк собраны, но НЕ используются (movок берёт keyword-пороги) | Порог по ключевому слову может расходиться с фактом |
| **Сверка univision ↔ adilet** | Только запланирована, кода нет | ❌ | grep `adilet` по *.py → только дисклеймер. README:9 «двумя методами» = два скрейпера ОДНОГО источника | **`pitch:306` пишет «сверка с adilet/testcenter» — читается как выполненная, хотя её нет** | **Высокий репутационный: витрина заявляет верификацию, которой нет** |
| Дубли квот в данных | 177 конфликтующих дублей по UNIQUE-ключу | ⚠️ | Проверка: `u5/B015/disability` score 89 vs 102; disability 137, rural 31; в general/passing дублей 0 | `scrape_all.py` неверно размечает квотные таблицы; `load_postgres.py:45` ON CONFLICT DO NOTHING потеряет 179 строк (5632→5453) | Мина под фичу квот и первый Postgres-запуск |
| requirements.txt POC | Нет fastapi/uvicorn/httpx | 🟡 | 4 пакета; `api/main.py:14` импортит fastapi; pandas указан, но не используется | README quickstart упадёт на чистой машине | Демо у второго человека сломается на шаге 1 |
| Смешение движков A/B | Сейчас невозможно | ✅ | Разные сторы (`grant_scores.db` vs `index.db`); заглушка A без доступа к данным | Будущий риск: docs/03 планирует ОДИН Postgres на оба | Средний, отложенный |
| Postgres-путь | Код есть, не запускался | ❔ | `docker ps` → daemon off; `import psycopg` fail | — | Средний |
| Устаревший текст | «POC-базе (5 вузов)» при 87 | ⚠️ | `recommend.py:119`; год захардкожен `:80` `year=2025` | Вводит в заблуждение | Низкий |

---

## D. Гигиена репозитория / инфраструктура / безопасность / тесты / логирование

| Область | Что найдено | Статус | Доказательство | Проблема | Риск |
|---|---|---|---|---|---|
| **Git-версионирование** | Нет репозитория вообще | ❌ | `git status` → `fatal: not a git repository`, exit 128 | Ни истории, ни отката, ни веток | **Любая правка безвозвратна; нет ревью/CI/bisect** |
| Кириллический путь | «ML Проекты» уже ломал инструменты | ⚠️ | `ocr_calendars.py:24` обход cv2; `reconfigure(utf-8)` в 8 файлах | Каждый новый инструмент (Docker/CI/git) — потенциальная мина | Накопительный; дешевле переехать до git init |
| Корневой README | Отсутствует | ❌ | `ls *.md` в корне → пусто; README только в 3 подпапках | Порядок запуска размазан | Проект не передаваем без экскурсии |
| Зависимости (воспроизводимость) | Только сломанный POC-requirements | 🟡 | Корневого нет; deps в pip-строках README; POC-req без fastapi | Окружение с нуля не собрать | «Works on my machine» |
| .gitignore | Отсутствует | ❌ | нет файла; в дереве __pycache__ ×4, *.db, raw (4.6M), .omc ×2 | При git init мусор уедет в индекс | Раздутый репо |
| .env.example | Отсутствует | ❌ | env раскиданы: OPENAI_API_KEY, DATABASE_URL, EMBED_PROFILE | Новый человек узнаёт из трейсбэков | Низкий, дешёвый фикс |
| Секреты в коде | Хардкодов нет | ✅ | grep sk-/api_key/password → только dev-дефолт postgres:postgres в POC compose | Только локальный dev-дефолт | Низкий (техдолг к деплою) |
| Тесты (покрытие) | Только POC-API (11 смоук) | 🟡 | Glob → 1 файл; ingestion/retrieval/isolation/refusal — 0 | Инварианты §2.4/§2.8 не покрыты | Регрессия молча убьёт инвариант |
| Логирование / observability | 0% (только print + batch-отчёты) | ❌ | grep logging/logger/Langfuse → No matches | Заявлено в цели A ТЗ; Langfuse в §12 | Этапы 3+ не отлаживаемы |
| Этап 3: LLM-генерация | Отсутствует | ❌ | grep openai/gpt-4/ollama в не-POC → только эмбеддинги; нет `generation/`, `providers/llm.py` | Нет «G» в RAG | — (не сделано по плану) |
| Этап 3: промпт/отказ в коде | Только текст в доке | ❌ | `docs/00:280-283` черновики; в *.py нет | Документация ≠ реализация | — |
| Этап 4: UI + FastAPI движка A | Отсутствует | ❌ | grep streamlit/chainlit → 0; FastAPI только POC (A = stub `main.py:36`) | RAG-пайплайн и API не соединены | — |

---

## E. Дрифт документации от кода (docs/00-foundation = «конституция», контракт для младшей модели)

| Ось расхождения | Заявлено в docs/00 | Фактически в коде | Статус | Доказательство |
|---|---|---|---|---|
| Парсер/чанкер | Docling HybridChunker (осн.), §7.3/ADR-004 | pymupdf4llm + собственный heading/table-сплиттер | ❌ дрифт | `ingest.py:76-77`; grep docling→только заглушка. В foundation замена НЕ признана |
| Структура репо §13 | api/, core/, generation/, ui/, eval/, tests/, `retrieval/pgvector_store.py` | Нет этих папок; файл — `retrieval/stores.py` | 🟡 дрифт | `ls` корня. **Инвариант §2.3 именует несуществующий `pgvector_store.py`** |
| Эмбеддинги | OpenAI 3072 (ADR-007, §14) | MiniLM 384 (SQLite) | 🟡 дрифт | `index.db` dim=384. Признано только в README подпапки, не в foundation |
| Vector DB | pgvector (ADR-016) | Прогон на SQLite FTS5+numpy; pgvector-класс не запускался | 🟡 дрифт | `stores.py`; README:3-4 признаёт, foundation молчит |
| Токенайзер §14 | `AutoTokenizer("BAAI/bge-m3")` «НЕ tiktoken» | `cl100k_base` (tiktoken) | ⚠️ противоречие | `kbtu.yaml:13`, `ingest.py:25`. **Если младшая модель «починит» по §14 → сдвиг всех chunk_id (риск №13)** |
| `embedding.profile` в YAML | — | `cloud` в YAML, но код читает CLI-арг (дефолт local) | ⚠️ мёртвый ключ | `kbtu.yaml:16` vs `index_chunks.py:25` |
| Версии доков | шапка v1.3 (дата 2026-06-25) | ADR-014..017 датированы 2026-07-02; docs/03 ссылается на v1.1 | ⚠️ | три разных номера версии в перекрёстных ссылках |
| Typer CLI + Pydantic-валидация §5/§7.5 | заявлено | argparse + yaml.safe_load без схемы | ❌ | `ingest.py:161-164` |
| Reranker (cross-encoder) §8 | часть слоя 2 | нет, только RRF→top_k | ❌ | `stores.py:114`; confidence-gate §9.1 завязан на несуществующий rerank-score |

> **Вывод по дрифту:** код нижнего уровня **честнее** документации — README подпапок точны и содержат разделы «Честно/ограничения». Но `00-foundation.md` (по §0 — контракт «архитектура меняется здесь раньше кода») систематически отстаёт по 4 осям. **Обязателен к обновлению ДО передачи задач младшей модели.**

---

## Сводка статусов по ТЗ-этапам

| Этап ТЗ | Статус | Комментарий |
|---|---|---|
| 1. Ingestion (парсинг, таблицы, чанкинг, метаданные) | 🟡 работает с оговорками | 531 чанк, метаданные полные, таблицы атомарны; НО 42% чанков <300 ток, 8.7% шум-дубли, 8 битых чанков, OCR-календари структурно повреждены |
| 2. Retrieval (вектор, гибрид, фильтры, цитаты) | 🟡 работает как демо | гибрид+цитаты подтверждены прогоном; НО утечка тенанта (баг), нет теста изоляции, MiniLM вместо прод-модели, pgvector не запускался |
| 3. Generation & Guardrails (LLM, промпт, отказ) | ❌ отсутствует | ноль вызовов LLM; промпт/отказ только в доке |
| 4. UI (Streamlit) + API движка A | ❌ отсутствует | движок A в API — заглушка |
| 5. Eval (Ragas) + observability | ❌ отсутствует | логирование 0%, тестов инвариантов нет |
| (Расширение) Движок B — рекомендатель | ✅ зрелый POC | 5632 строки, тесты PASS, честность в коде; но «сверка с adilet» заявлена ложно, 177 дублей квот |

**Итог:** этапы 1-2 реализованы как честное демо с исправимыми дефектами; этапы 3-5 отсутствуют (по плану); движок B — самая зрелая часть, но с одной репутационной миной (ложная провенанс-строка). Проект **как код** аккуратен, но **как репозиторий не существует** (нет git/README/requirements).
